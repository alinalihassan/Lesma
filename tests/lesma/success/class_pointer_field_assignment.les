class IntPtrBox
    var ptr: *int

    def new(ptr: *int)
        self.ptr = ptr

var value = 101
var box = IntPtrBox(&value)

if box.ptr is not *int
    exit(1)

var other = 77
box.ptr = &other
if box.ptr is not *int
    exit(1)
