# Subclass constructor calls base `new` via super.

class Base
    var x: int

    def new()
        self.x = 7

class Child: Base
    def new()
        super.new(self)

var c = Child()
if c.x != 7
    exit(1)
