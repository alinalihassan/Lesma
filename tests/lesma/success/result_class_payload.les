class Box {
  var value: int

  func new(value: int) {
    self.value = value
  }
}

let ok: Result<Box, str> = Result.Ok(Box(4))
let maybe = ok.unwrap()
if maybe is Box {
  let box = maybe as Box
  if box.value != 4 {
    exit(1)
  }
} else {
  exit(2)
}
