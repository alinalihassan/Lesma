# Vtable slot keys ignore the return type; overrides must still match the full inherited signature.

class Base {
    func id() -> int {
        return 1

    }
}
class Child: Base {
    func new() {
        super.new(self)

    }
    overload func id() -> float {
        return 1.0

    }
}
