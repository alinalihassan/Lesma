# Mutating a list through an immutable class field must be rejected.
class Holder {
  let nums: list<int> = [1]
  func tryMutate() -> void {
    self.nums.push(2)
  }
}
var h = Holder()
h.tryMutate()
