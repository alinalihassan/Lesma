enum Color {
  RED
  BLUE
  GREEN
}
var x: Color = Color.RED

func checkEnum(x: Color) -> bool {
  if Color.BLUE == Color.BLUE and Color.GREEN != Color.RED and x == Color.RED {
    return true
  }
  return false
}

if not checkEnum(x) {
  exit(1)
}
