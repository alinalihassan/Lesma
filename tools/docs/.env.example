# Optional: separate playground origin (no trailing slash). Omit to use the in-app /playground embed and /playground/ static bundle on the same host.
# VITE_PLAYGROUND_URL=https://play.lesma-lang.com
