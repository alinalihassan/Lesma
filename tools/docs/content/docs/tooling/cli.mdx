---
title: CLI Reference
description: |
  lesma run, lesma compile, optimization flags, and common workflows.
category: Tooling
---

The **`lesma`** executable is the user-facing compiler driver.

## `lesma run`

Compile and execute a program via LLVM JIT (typical development flow):

```bash
lesma run path/to/file.les
```

Optimization level can be set with flags such as `-O0` … `-O3`; run `lesma --help` for the current list.

## `lesma compile`

Emit object files for linking:

```bash
lesma compile path/to/file.les -o out.o
```

## Version

```bash
lesma --version
```

## Next steps

- [Build From Source](/docs/getting-started/build-from-source) if you are compiling Lesma yourself.
- [Your First Program](/docs/getting-started/first-program) for a minimal edit–run loop.
